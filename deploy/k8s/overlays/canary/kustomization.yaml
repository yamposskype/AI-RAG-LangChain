apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - rollout-backend.yaml
  - rollout-rag-app.yaml
  - rollout-frontend.yaml

patches:
  - path: delete-deployments.yaml
  - path: hpa-rollout-targets.yaml
